# Directory containing trajectory data (subdirectories like pendulum_lqr_50k, cartpole_pybullet, etc.)
DATA_DIR=/path/to/data_trajectories

# Directory for experiment outputs (model checkpoints, logs, etc.)
OUTPUT_DIR=outputs
